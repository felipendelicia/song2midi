# Windows scripts must keep CRLF: cmd.exe mis-parses LF-only batch files,
# and a .ps1 checked out with LF confuses some Windows editors.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# Everything else stays LF in the repo.
* text=auto eol=lf
